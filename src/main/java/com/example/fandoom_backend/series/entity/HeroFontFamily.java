package com.example.fandoom_backend.series.entity;

// Blog'un BlockFontFamily'sinden bilinçli olarak bağımsız — series/
// modülü blog/'a bağımlı olmasın, ileride Hero'ya özel yeni font eklemek
// Blog'u etkilemesin.
public enum HeroFontFamily {
    COOPER_BT
}
