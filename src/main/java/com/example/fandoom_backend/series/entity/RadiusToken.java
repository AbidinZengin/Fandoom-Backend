package com.example.fandoom_backend.series.entity;

// FE'nin mevcut --radius-* CSS token adlarıyla birebir eşleşir — keyfi
// px değeri değil, tasarım sistemiyle tutarlılık için.
public enum RadiusToken {
    SM, MD, LG, PILL
}
