package com.example.fandoom_backend.lore.entity;

// person/'daki Character.SubjectType ile aynı kavram, kasıtlı olarak ayrı
// kopya: modül bağımsızlığı kuralı gereği lore/ person/'ın entity paketine
// bağımlı olamaz.
public enum SubjectType {
    MOVIE, SERIES
}
