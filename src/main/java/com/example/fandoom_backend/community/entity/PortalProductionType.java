package com.example.fandoom_backend.community.entity;

// community/'nin kendi enum'u: person/blog/lore'daki SubjectType'lara bağımlılık kurulmaz (modül bağımsızlığı).
public enum PortalProductionType {
    MOVIE, SERIES
}
