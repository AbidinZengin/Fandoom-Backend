package com.example.fandoom_backend.group.entity;

// person/'daki Character.SubjectType ile aynı kavram, kasıtlı olarak ayrı
// kopya: modül bağımsızlığı kuralı gereği group/ person/'ın entity paketine
// bağımlı olamaz.
public enum SubjectType {
    MOVIE, SERIES
}
